{
  "name": "t3code-mcp",
  "version": "0.2.0",
  "description": "MCP server for controlling local and T3 Connect environments: list threads, read history, send messages, handle approvals.",
  "type": "module",
  "bin": {
    "t3code-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "dev": "tsc --watch",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
